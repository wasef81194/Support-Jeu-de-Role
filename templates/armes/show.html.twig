{% extends 'base.html.twig' %}

{% block title %}Armes{% endblock %}

{% block body %}
    <h1>Armes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arme.id }}</td>
            </tr>
            <tr>
                <th>Arme</th>
                <td>{{ arme.arme }}</td>
            </tr>
            <tr>
                <th>Competence</th>
                <td>{{ arme.competence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_armes_index') }}">back to list</a>

    <a href="{{ path('app_armes_edit', {'id': arme.id}) }}">edit</a>

    {{ include('armes/_delete_form.html.twig') }}
{% endblock %}
